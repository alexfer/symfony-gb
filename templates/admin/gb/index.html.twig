{% extends 'base.html.twig' %}

{% block title %}{{ 'action.entries'|trans }}{% endblock %}

{% block body %}    
    <div class="container">        
        <div class="card border-secondary mt-5">
            <div class="card-header">
                <h5>{{ 'action.entries'|trans }}</h5>                   
            </div>
            <div class="card-body">
                {% if app.user %}
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>User Name</th>
                                <th>Title</th>
                                <th>Created At</th>
                                <th>Updated At</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for gb in g_bs %}
                                <tr{{ gb.approved == 0 ? ' class="table-muted text-muted"': '' }}>
                                    <td>{{ gb.id }}</td>
                                    <td>
                                        <a class="link-secondary" href="{{ path('app_admin_user_show', {'id': gb.user.id}) }}">{{ gb.user.name }}</a>
                                    </td>
                                    <td>{{ gb.title }}</td>        
                                    <td>{{ gb.createdAt ? gb.createdAt|date('Y-m-d H:i') : '' }}</td> 
                                    <td>{{ gb.updatedAt ? gb.updatedAt|date('Y-m-d H:i') : '' }}</td>
                                    <td class="text-center">
                                        <a class="link-secondary text-decoration-none" href="{{ path('app_admin_entry_edit', {'uuid': gb.uuid}) }}"><i class="fa fa-pencil-square-o"></i></a> 
                                        <a class="link-secondary text-decoration-none" href="{{ path('app_admin_entry_show', {'uuid': gb.uuid}) }}"><i class="fa fa-eye"></i></a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="6" class="text-center">{{ 'entry.no_entries_found'|trans }}</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% endif %}
            </div>
            <div class="card-footer">
                <a class="btn btn-secondary float-right" href="{{ path('app_g_b_new') }}">{{ 'action.create_entry'|trans }}</a>
            </div>
        </div>
    </div>
{% endblock %}
