{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit User</h2>        
        {% include 'flashMessage/flashMessage.html.twig' %}        
        <div class="card">
            <div class="card-header">
                <a class="btn btn-secondary float-end" href="{{ path('app_admin_index') }}"> 
                    Cancell
                </a>
            </div>
            <div class="card-body">
                {% include 'admin/user/_form.html.twig' with {'button_label': 'Update', 'button_color': 'btn-primary'}  %}
            </div>
        </div>
    </div>
{% endblock %}