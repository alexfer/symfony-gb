<form method="post" action="{{ path('app_g_b_delete', {'id': g_b.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ g_b.id) }}">
    <button class="btn">Delete</button>
</form>
