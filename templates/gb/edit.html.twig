{% extends 'base.html.twig' %}
  
{% block title %}Edit Project{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Entry</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-secondary float-end" href="{{ path('app_g_b_index') }}"> 
                    View All
                </a>
            </div>
            <div class="card-body">
                {% include 'gb/_form.html.twig' with {'button_label': 'Update', 'button_color': 'btn-primary'}  %}
            </div>
        </div>
    </div>
{% endblock %}